apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-search
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-search
  template:
    metadata:
      labels:
        app: app-search
    spec:
      containers:
      - name: app-search
        # image: me-west1-docker.pkg.dev/hackathon-082024/hackathon-repo/<IMG>:latest
        ports:
        - containerPort: 8080
        # envFrom:
        # - secretRef:
        #     name: secret-backend
        # - configMapRef:
        #     name: configmap-backend

---

apiVersion: v1
kind: Service
metadata:
  name: search-service
spec:
  type: ClusterIP
  selector:
    app: app-search
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8000
